import styled from "styled-components";

export const ButtonContainer = styled.div`
  position: fixed;
  right: 64px;

  img {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    cursor: pointer;
  }
`;
